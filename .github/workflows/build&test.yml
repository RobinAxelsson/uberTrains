# This workflow will do a clean install of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: UberTrains Build & Test

on:
  push:
   branches-ignore:
   - entityModels

jobs:
  build-backend:
      runs-on: ubuntu-latest

      steps:
      - uses: actions/checkout@v2

      - name: Install modules
        run: cd backend && npm install

      - name: Run build
        run: cd backend && npm run build

  build-frontend:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2

    - name: Install modules
      run: cd frontend && npm install
    
    - name: Build Frontend
      run: cd frontend && npm run build
        
  Unit-Testing-Jest-Backend:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install modules
      run: cd backend && npm install

    - name: Run tests
      run: cd backend && npx jest

  Newman-Integration-Tests:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install modules
      run: cd backend && npm install # NOTE: casing of Backend backend is error source.

    - name: Test Api Endpoints with Newman
      run: bash ./scripts/newman-endpoint-job.sh ${{ secrets.COLLECTION_UID }} ${{ secrets.POSTMAN_APIKEY }}